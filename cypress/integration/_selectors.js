const inputExpectedAge = 'input[name=expectedAge]'
const inputDateOfBirth = 'input[name=dateOfBirth]'
const inputName = 'input[name=name]'
const submitButton = 'button[type=submit]'
const clearButton = '#clear_button'
const howto_title = '#howto_title'
const englishButton = '#lang_en'
const frenchButton = '#lang_fr'
const resultMessage = '#result'

export {
    inputDateOfBirth,
    inputExpectedAge,
    inputName,
    submitButton,
    clearButton,
    howto_title,
    englishButton,
    frenchButton,
    resultMessage,
}
